meta {
  name: Health
  type: http
  seq: 1
}

get {
  url: {{fastifyUrl}}/health
  body: none
  auth: none
}

headers {
  Accept: application/json
}

docs {
  Public health check — no API key required.
  
  Returns:
  - `status` - ok
  - `middleware` - fastify
}
